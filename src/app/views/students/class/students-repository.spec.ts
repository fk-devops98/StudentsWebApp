import { StudentsRepository } from './students-repository';

describe('StudentsRepository', () => {
  it('should create an instance', () => {
    expect(new StudentsRepository()).toBeTruthy();
  });
});
